# Atya.Foundation.Time

UTC-safe time abstractions for foundation packages that need injectable clocks
and explicit validation around UTC usage.

## Included APIs

- `IClock`
- `SystemClock`
- `TimeGuard.AgainstNonUtc`

## Install

```bash
dotnet add package Atya.Foundation.Time
```
